services:
  iimcs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iimcs
    ports:
      - "${HOST_PORT:-8012}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
